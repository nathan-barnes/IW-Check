<%# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
  # ImageWall Gallery (Projects) Show
  # location: views/gallery/show.html.erb
  # - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
%>

<%# Meta Tags %>
<% content_for :metatags do %>
  <%= render partial: "partials/head/metatags", locals: {
    url: "https://www.imagewall.com/gallery/#{@project.slug}",
    title: "#{@project.title} — ImageWall",
    description: "#{@project.description}",
    image: @project.media.first.url
  } %>
<% end %>

<%= render partial: "partials/project", locals: { project: @project, modal: false } %>